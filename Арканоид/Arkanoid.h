//#ifndef ARKANOID_H_INCLUDED
//#define ARKANOID_H_INCLUDED

//#include <windows.h>

/*class Arkanoid {
public:
	//static HACCEL _hAccelTable;
	static const char* lpCmdLine;

	static LRESULT CALLBACK wndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam);

	static LRESULT onCommand(USHORT wNotifyCode, USHORT wID, HWND hwndCtl);
	static LRESULT onKeyDown(int nVirtKey, LPARAM lKeyData);
	static LRESULT onKeyUp(int nVirtKey, LPARAM lKeyData);
	static LRESULT onSize(long fwSizeType, short nWidth, short nHeight);
	static LRESULT onMove(int xPos, int yPos);
	static LRESULT onDestroy();
	static LPCTSTR szTitle;
	static int nExitCode;

	static void init(HINSTANCE hInstance,
		HINSTANCE hPrevInstance,
		LPSTR lpCmdLine,
		int nCmdShow);
	static void run();
	static void release();
};*/

//#endif	// ARKANOID_H_INCLUDED